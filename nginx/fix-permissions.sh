#!/bin/bash

# Скрипт для непрерывного мониторинга и исправления прав доступа к HLS файлам

HLS_DIR="/var/www/streams/hls"

echo "🔧 Запуск мониторинга прав доступа для HLS..."

while true; do
    # Исправляем права для всех файлов и папок
    if [ -d "$HLS_DIR" ]; then
        find "$HLS_DIR" -type d -exec chmod 755 {} \; 2>/dev/null
        find "$HLS_DIR" -type f -exec chmod 644 {} \; 2>/dev/null
    fi
    
    # Проверяем каждые 2 секунды
    sleep 2
done

